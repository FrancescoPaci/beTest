<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="pax.test.mybatis.custom.OrdersCustomMapper">

    <select id="selectDistinctByColumnFromXml" resultType="java.lang.String">
        SELECT DISTINCT ${column} FROM orders
    </select>

</mapper>